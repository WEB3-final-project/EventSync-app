# EventSync-app
## User Guide
 - Go on terminal and tap cd backend/pnpm install for backend dependencies
 - Go on terminal and tap cd frontend/pnpm install for frontend dependencies
 - Tap backend/pnpm exec tsx server.js to run backend
 - Tap frontend/pnpm dev to run frontend
